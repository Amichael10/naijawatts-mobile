/**
 * NaijaWatts — Theme barrel export
 */

export { theme, tenantColors, getTenantColor, fonts, fontSizes } from './constants';
export type { ThemeColors, ThemeMode } from './constants';
export { ThemeProvider, useTheme } from './ThemeContext';
export {
    radii,
    cardStyle,
    primaryButtonStyle,
    primaryButtonTextStyle,
    secondaryButtonStyle,
    secondaryButtonTextStyle,
    inputStyle,
    inputTextStyle,
    chipStyle,
    chipTextStyle,
    typography,
} from './globalStyles';
